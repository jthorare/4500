# Outline

+ 1-in.json : > 10 cards, return first legally acquirable connection (first connection)
+ 2-in.json : > 10 cards, get first legally acquirable connection (non-first connection)
+ 3-in.json : <= 10 cards
+ 4-in.json : 0 rails
+ 5-in.json : >10 cards, enough rails, but not enough cards of the right color to acquire any of the connections
